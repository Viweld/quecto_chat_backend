name: quecto_chat_backend
description: A new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"


dependencies:
  dart_frog: ^1.1.0
  http: ^1.2.2
  collection: ^1.19.0
  postgres: ^3.4.0
  dotenv: ^4.2.0
  dart_jsonwebtoken: ^2.14.1
  json_annotation: ^4.9.0
  intl: ^0.19.0
  intl_translation: ^0.20.0


dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.5
  build_runner: ^2.4.13
  json_serializable: ^6.8.0
  intl_utils: ^2.8.7

flutter_intl:
  enabled: false # Required. Must be set to true to activate the package. Default: false
  class_name: Localization # Optional. Sets the name for the generated localization class. Default: S
  main_locale: en # Optional. Sets the main locale used for generating localization files. Provided value should consist of language code and optional script and country codes separated with underscore (e.g. 'en', 'en_GB', 'zh_Hans', 'zh_Hans_CN'). Default: en
  arb_dir: lib/l10n # Optional. Sets the directory of your ARB resource files. Provided value should be a valid path on your system. Default: lib/l10n
  output_dir: lib/l10n/generated # Optional. Sets the directory of generated localization files. Provided value should be a valid path on your system. Default: lib/generated
  use_deferred_loading: false # Optional. Must be set to true to generate localization code that is loaded with deferred loading. Default: false
